# 古典密码三层加密破解程序分析报告

## 问题1：列置换排列枚举的时间复杂度

程序在步骤1中枚举列置换排列时，存在两个阶段：

**阶段A（快速路径）**：使用 `next_permutation` 枚举排列，排列数为 **m!**

**阶段B（穷举回退）**：穷举所有长度为 m 的无重复字母密钥，排列数为 **P(26, m) = 26!/(26-m)!**

**最坏情况时间复杂度**：O(P(26, m)) = O(26!/(26-m)!)

当密钥长度 **m=6** 时：
- 阶段A排列数：6! = **720 种**
- 阶段B排列数：P(26,6) = 26×25×24×23×22×21 = **165,765,600 种**

代码位置：`cracker.h:275-339`

---

## 问题2：弗里德曼试验 IC 阈值分析

**IC阈值设为0.04的依据**：
- 英文自然文本的重合指数（IC）约为 **0.067**
- 随机文本（均匀分布）的 IC 约为 **1/26 ≈ 0.0385**
- 0.04 是随机文本 IC 的近似值，作为区分"类随机文本"和"类英文文本"的分界点
- 代码中实际使用 `abs(IC - 0.067) < 0.015` 作为判断标准（`cracker.h:196`）

**将阈值提高到0.05的风险**：
- **漏报率（False Negative）增加**：正确的密钥长度可能因IC值略低于0.05而被错误排除
- 维吉尼亚密码解密后的分组文本IC值通常不会完美达到0.067，设置过高阈值会导致有效候选被过滤
- 可能需要更多次穷举尝试才能找到正确密钥，显著降低破解效率

代码位置：`analysis.h:67-83`

---

## 问题3：Hill密钥恢复的可逆性保证

**如何保证明文块矩阵P可逆**：
1. **预计算筛选**：`findInvertibleCombos` 函数（`cracker.h:52-66`）预先遍历所有3块组合
2. **可逆性检查**：使用 `gcd(det3x3(P), 26) == 1` 作为判断条件（`cracker.h:62`）
   - 模26下矩阵可逆的充要条件是行列式与26互质（即gcd(det,26)=1）
3. **双重验证**：在 `tryRecoverHillKey` 中（`cracker.h:83-85`）再次检查可逆性并实际求逆

**所有3组块都不可逆时的处理**：
- `validCombos` 向量将为空（`cracker.h:258`）
- `tryFullDecrypt` 中的循环（`cracker.h:149`）不会执行，直接返回 `false`
- 程序继续尝试下一个候选密钥
- 如果所有密钥长度的所有候选都失败，最终输出"破解失败"

代码位置：`cracker.h:52-95, 149-177`

---

## 问题4：整数溢出风险分析

**存在潜在的整数溢出风险**，主要位于以下位置：

### 风险位置1：3×3行列式计算
**文件**：`math_utils.h:28-33`
```cpp
int d = m[0][0] * (m[1][1] * m[2][2] - m[1][2] * m[2][1])
      - m[0][1] * (m[1][0] * m[2][2] - m[1][2] * m[2][0])
      + m[0][2] * (m[1][0] * m[2][1] - m[1][1] * m[2][0]);
```
**触发条件**：虽然单个矩阵元素在0-25范围内，但连续乘法运算（25×25×25=15,625）加上正负号组合，理论上不会溢出32位int（范围约±2×10⁹）。

### 风险位置2：伴随矩阵计算
**文件**：`math_utils.h:41-50`
```cpp
adj[0][0] =  (m[1][1] * m[2][2] - m[1][2] * m[2][1]);
```
**触发条件**：与行列式计算类似，单个元素计算在安全范围内。

### 风险位置3：Hill加密/解密中的累加和
**文件**：`hill.h:20-23, 38-41`
```cpp
int sum = 0;
for (int col = 0; col < 3; col++)
    sum += k[row][col] * (s[i + col] - 'A');
```
**触发条件**：3次乘法累加（最大约 25×25×3 = 1,875），在int安全范围内。

### 结论：
当前代码在32位int环境下**实际溢出风险较低**，所有计算结果均在合理范围内。但如果扩展到更大的矩阵尺寸（如n>5）或使用16位int编译器，则可能发生溢出。
