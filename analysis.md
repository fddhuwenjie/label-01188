# 古典密码三层加密破解程序分析报告

## 问题1：列置换排列枚举的时间复杂度

### 最坏情况下的时间复杂度
程序在步骤1中枚举列置换排列时采用了**两阶段策略**：

**阶段A**（快速路径）：枚举所有 m! 种排列，通过 `next_permutation` 生成。
**阶段B**（回退穷举）：如果快速路径失败，穷举所有长度为 m 的无重复字母密钥，即从26个字母中选m个的排列数：P(26, m) = 26×25×24×...×(26-m+1)。

**最坏时间复杂度**为 **O(P(26, m))** = O(26!/(26-m)!)，这是指数级的复杂度。

### 当密钥长度 m=6 时的枚举数量
- **阶段A**：6! = **720** 种排列
- **阶段B**：26×25×24×23×22×21 = **165,765,600** 种密钥

---

## 问题2：弗里德曼试验 IC 阈值分析

### IC 阈值设为 0.04 的依据
从 `analysis.h:79-80` 可以看出，程序实际上是将 IC 值与 **0.067** 进行比较排序：
```cpp
sort(results.begin(), results.end(), [](auto& a, auto& b) {
    return std::abs(a.second - 0.067) < std::abs(b.second - 0.067);
});
```

**依据**：
- 英文自然文本的重合指数（IC）约为 **0.0667**
- 完全随机文本的 IC 约为 **1/26 ≈ 0.0385**
- 0.04 作为阈值是这两个值的分界点附近，用于区分"类英文文本"和"随机文本"

### 将阈值提高到 0.05 的风险
如果将阈值提高到 0.05：
1. **漏检风险**：维吉尼亚密码解密后的文本 IC 通常在 0.04-0.06 之间，提高阈值可能将正确的密钥长度排除在候选之外
2. **假阴性增加**：对于较短的密文或分布不太均匀的明文，IC 可能偏低，高阈值会导致漏检
3. **候选集缩小**：可能过早排除潜在的正确密钥长度，导致后续破解失败

---

## 问题3：Hill 密钥恢复的可逆性保证

### 如何保证明文块矩阵 P 可逆
从 `cracker.h:52-66` 的 `findInvertibleCombos` 函数可以看出：

1. **预计算所有可逆组合**：在破解开始前，程序遍历所有可能的3块组合
2. **可逆性判断**：通过 `gcd(det3x3(P), 26) == 1` 来判断矩阵 P 在模26下是否可逆
3. **只保留有效组合**：将所有满足条件的块索引组合存入 `validCombos` 向量供后续使用

```cpp
if (gcd(det3x3(P), 26) == 1)
    validCombos.push_back({i, j, k});
```

### 如果所有3组块都不可逆的处理
如果所有可选的3组块都不可逆：
1. `validCombos` 向量将为空
2. 在 `tryFullDecrypt` 函数（第149行）中，`for (auto& combo : validCombos)` 循环不会执行
3. 函数直接返回 `false`，当前密钥被跳过
4. 程序继续尝试其他候选密钥
5. 如果所有密钥长度的所有密钥都尝试完毕仍未找到解，最终输出"破解失败"

---

## 问题4：潜在的整数溢出风险

### 风险位置及触发条件

#### 风险位置1：行列式计算（`math_utils.h:28-33`）
```cpp
inline int det3x3(int m[3][3]) {
    int d = m[0][0] * (m[1][1] * m[2][2] - m[1][2] * m[2][1])
          - m[0][1] * (m[1][0] * m[2][2] - m[1][2] * m[2][0])
          + m[0][2] * (m[1][0] * m[2][1] - m[1][1] * m[2][0]);
    return mod(d, 26);
}
```

**触发条件**：当矩阵元素较大（接近25）且符号组合导致中间值超过 `int` 范围时。3个25相乘 = 15,625，虽然在32位 int 范围内，但在更复杂的计算中存在风险。

#### 风险位置2：伴随矩阵计算（`math_utils.h:42-50`）
```cpp
adj[0][0] =  (m[1][1] * m[2][2] - m[1][2] * m[2][1]);
// ... 共9个类似的计算
```

**触发条件**：与行列式计算类似，中间乘积可能溢出。

#### 风险位置3：矩阵乘法（`hill.h:22` 和 `hill.h:40`）
```cpp
sum += k[row][col] * (s[i + col] - 'A');  // hill.h:22
sum += ki[row][col] * (c[i + col] - 'A'); // hill.h:40
```

**触发条件**：虽然单次计算是 25×25=625，累加3次仅1875，不会溢出，但如果块大小增大或在其他上下文中使用可能存在风险。

#### 风险位置4：Hill密钥恢复矩阵乘法（`cracker.h:89`）
```cpp
for (int k = 0; k < 3; k++) s += C[i][k] * Pinv[k][j];
```

**触发条件**：C 和 Pinv 元素都是 0-25，3次累加最大为 3×25×25=1,875，相对安全，但理论上仍存在溢出可能性。

### 总结
最主要的溢出风险在 **`math_utils.h` 的行列式和伴随矩阵计算**中，虽然在当前3×3矩阵和模26场景下溢出概率较低，但代码设计上没有采取溢出防护措施。
