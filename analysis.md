# 古典密码三层加密破解程序分析报告

## 1. 列置换排列枚举时间复杂度分析
### 时间复杂度
程序步骤1中枚举列置换排列的最坏时间复杂度为 **O(m!)**，其中m为密钥长度。这是因为程序使用`next_permutation`枚举所有长度为m的排列组合，排列总数为m的阶乘。

### m=6时的排列数
当密钥长度m=6时，需要枚举的排列数为：
```
6! = 720 种排列
```

注意：这仅为阶段A的排列枚举次数，若阶段A未命中，程序会进入阶段B的全字母穷举，此时最坏情况需要枚举26*25*24*23*22*21 = 165,765,600 种密钥组合。

---

## 2. 弗里德曼试验IC阈值分析
### 阈值0.04的依据
英文文本的重合指数IC理论值为 **0.0667**，而完全随机文本的IC值约为 **0.0385**。程序中将阈值设定为0.04（实际代码中使用与0.067的差值<0.015作为判断标准），是为了区分自然英文文本和随机文本：
- 当IC值接近0.067时，说明文本是自然语言
- 当IC值接近0.0385时，说明文本是随机序列

0.04的阈值介于两者之间，能够有效过滤掉明显随机的文本，同时保留可能的正确候选。

### 阈值提高到0.05的风险
如果将阈值提高到0.05：
1. **漏报风险增加**：正确的解密结果可能因为IC值略低于0.05而被过滤掉，尤其是当密文较短或包含特殊字符时
2. **有效候选减少**：虽然可以减少后续需要处理的候选数量，但会显著增加丢失正确密钥的概率
3. **对密文长度更敏感**：短密文的IC值波动更大，更容易被错误过滤

---

## 3. Hill密钥恢复中P矩阵可逆性分析
### 可逆性保证
代码中通过以下方式保证所选明文块矩阵P在mod 26下可逆：
1. **预计算所有可逆组合**：`findInvertibleCombos`函数遍历所有可能的3个明文块组合，计算每个组合的3x3矩阵行列式d
2. **可逆性检查**：只有满足`gcd(d, 26) == 1`的组合才会被加入有效组合列表。因为模26下矩阵可逆的充要条件是行列式与26互质（26的质因子为2和13）

### 所有块都不可逆的处理
如果所有可选的3组块都不可逆：
1. `validCombos`列表为空
2. `tryFullDecrypt`函数中循环无法执行，直接返回false
3. 程序会继续尝试其他密钥，最终在所有密钥长度都尝试完成后输出"破解失败"

实际运行时，程序会在启动时输出"可逆块组合数: X"，如果X为0则说明已知明文不足以进行Hill密钥恢复。

---

## 4. 潜在整数溢出风险分析
程序中存在两处明确的整数溢出风险：

### 风险位置1：3x3矩阵行列式计算（math_utils.h第23-26行）
```cpp
inline int det3x3(int m[3][3]) {
    int d = m[0][0] * (m[1][1] * m[2][2] - m[1][2] * m[2][1])
          - m[0][1] * (m[1][0] * m[2][2] - m[1][2] * m[2][0])
          + m[0][2] * (m[1][0] * m[2][1] - m[1][1] * m[2][0]);
    return mod(d, 26);
}
```
**触发条件**：
- 矩阵元素最大值为25，三个元素相乘最大为25*25*25 = 15625
- 三个这样的项相加减，最大值约为3*15625 = 46875
- 虽然在当前32位int系统下不会溢出，但如果扩展到更大的矩阵或更大的模数时，存在溢出风险

### 风险位置2：矩阵乘法计算（hill.h第22-25行和第41-44行）
```cpp
// hillEnc中的计算
sum += k[row][col] * (s[i + col] - 'A');

// hillDec中的计算
sum += ki[row][col] * (c[i + col] - 'A');
```
**触发条件**：
- 3x3矩阵相乘时，每个sum是3个(25*25)的和，最大为3*625=1875，当前不会溢出
- 如果扩展到更大的矩阵（如4x4及以上），sum值会快速增长，超过int范围导致溢出

### 其他潜在风险
- 密文长度如果超过INT_MAX（约20亿），字符串长度的size_t到int的转换会溢出
- 实际场景下密文长度通常不会达到这个量级，但理论上存在风险
